# vLLM 0.30-on-Windows 迁移 — 进度文档

> 截至 2026-09-26（步骤 018，三线合并回归收官——阶段 2 验收总账结清，生产等价判定成立）。每步细节见《实验步骤文档.md》，每日流水见《实验日志.md》，规划见《vllm-030win-迁移计划.md》。
> deadline 2026-10-30（新 27B 模型发布即止损）。

## 一、阶段总览

| 阶段 | 状态 | 关键成果 |
|---|---|---|
| 阶段 0 准备与锚点 | ✅ **收官** | 锚点数据包归档（A1 PPL/A2-A4/A5 长上下文/A5b 无草稿基线）；cp312 venv 建成；底座 git 化（baseline `9948275`） |
| 阶段 1 底座冒烟 | ✅ **收官（步骤 011）** | S1/S2 关键判定双过；S3a 共存+A6 显存账 ✅（shared 零增长无溢出）；S4/A8 tool-call+reasoning ✅（fp8 KV 跑通）；S6 长稳 20/20 ✅。两个结构缺口定性：投机解码不可用（SM120 静态 XQA 且上游未接线）挂批 4；图模式不可用（humming×compile）挂批 3b |
| 阶段 2 自研迁移+验收 | ✅ **收官（2026-09-26 步骤 018，三线合并回归全过）** | 批1-5 全落地（`57188ef`…`78db8a6`，各批 PPL 8/8 带内）；**三线合并回归 ✅**：线A 无 spec 四档超锚（56.12/55.97/54.32/50.38 vs 55.7/55.6/54.2/50.3）、线B spec 端到端三锚全过（A2 73.00 vs 60.65；A5 68.59/41.45/24.39=+19~29%；A4 接受率 0.72-0.80 带内偏上）、线C 图模式合并覆盖；PPL 复锚 8/8 带内；两回归修复 `6fc2108`（INC 派发错位）+`e9d534d`（HummingLinearMethod 过 custom-op） |
| 阶段 3 0.30 甄选滚动 | ⚪ 未开 | 清单已定（A组+B7+D组+C1/C6/C8，砍尾 C8→C1/C6→D） |
| 阶段 4 全量回归+切换 | ✅ **切换完成 + R8 闭环（步骤 020/021）** | 生产已切 0.29 栈（`run_dflash2_n2_base029.cmd`，四件验证/日志三行 ✅，pool 114,974 无缩水）；**R8 KV offload 崩溃已修复回填**（底座 `d7cdb91`，多轮实证 32k 二轮 TTFT 21.0→3.1s）；vision 面未切换（A7 锚挂账） |
| 阶段 5 收尾 | ⚪ 未开 | — |

## 二、任务板

- **下一步（按序）**：
  1. **⭐快慢态孤例定罪（现头名，潜在再降 ~24%）**：20.85ms 快态同产物未复现（26.97/27.08），机制在运行时层（候选：GPU/CPU 争用、flashinfer autotune 选型噪声〔缓存从不持久〕、睿频/核驻留）；≥6 boot 采样态分布
  2. CPU 派发优化续（attention 226µs×21/kv_update 329µs×21/apply_humming 95µs 单价）；0.30 甄选滚动（A组+B7+D组+C1/C6/C8）；A7 vision 锚采证
  3. 10-30 止损线
- **⭐待优化项（用户 2026-09-26 指示登记）**：
  1. ~~【高优先】KV offload×0.29 Windows 崩溃修复~~ **✅ 已闭环（步骤 021，底座 `d7cdb91`）**：根因=**cuMemcpyBatchAsync 驱动级缺陷（非默认流使用即毒化上下文）**；修复=`_custom_ops.swap_blocks_batch` Windows 分支 ctypes 逐条 `cuMemcpyAsync`；已回填生产（offload 两行回来）；多轮实证 32k 二轮 TTFT 21.0→3.1s。
  2. **【现头名】T-perf CPU 派发优化战役**（诊断已收官步骤 022）：靶点=图外 CPU 派发（GDN 包装 244µs×48/步、KV update 329µs×21、attention 226µs×21、CompiledFxGraph 7.5ms/步）；GPU 已贴权重读地板 14.9ms/步；估 30.7→~20ms 空间。
  3. A7 vision 131k/155k 锚采证（vision 面切换前置）。
- **切换演练已收官（2026-09-26 步骤 019）**：生产 launcher `tools/serve_gsq_prod029_n2.cmd` + 模型目录入口 shim；最终配置冒烟全绿（reasoning/tool-call/8k+32k needle/prefix 命中 TTFT 5.78→3.09s/共享显存 0 MB）；**KV offload×0.29 Windows 崩溃三探针定罪**（全量崩→去 spec 同崩→去 offload 全绿；钉页排除）——生产定格无 offload（容量 121,058 tokens 不变、锚本就无 offload 口径）；offload Windows 移植层四坑已修（底座 `b315e77`+1），残余缺陷挂账（见上方待优化项）
- **三线合并回归已收官（2026-09-26 步骤 018）**：线A 无 spec 四档超锚、线B spec 端到端 A2/A4/A5 三锚全过、线C 图模式合并覆盖、PPL 复锚 8/8 带内；两回归修复 `6fc2108`（INC 低 bit→humming 派发误插 CPU 分支 bits==4 守卫内=双重死代码）+ `e9d534d`（HummingLinearMethod.apply 直调 humming_forward 未过 custom-op 边界）。数据=`锚点数据包/lineA_*`、`lineB_*`、`ppl_anchor_base029_lineBfix_r5.json`。
- **批 5 已收官（2026-09-26，步骤 017，阶段 2 五批全收官）**：底座 `78db8a6`（+368/−39）。覆盖性判定：utils/KV spec 层/E6.1 零搬运；落地 11 组 hunk（use_fa2_nvfp4_kv 机制 + dedicated XQA 接 nvfp4 + E6.2 writer + split-kv gate）。**flashinfer 0.6.11→0.6.18.post1**（生产同款纯 py 包整目录拷贝；0.6.11 的 FA2 fp4 reader 未经生产验证 + reference writer 兜底被位等价探针证伪）。runner 要点：FLASHINFER_WORKSPACE_BASE=C:/fw（0.6.18 把 `\f` 吃成 form-feed）+ FLASHINFER_EXTRA_LDFLAGS=-ltvm_ffi。**PPL 8/8 全绿（A1 同 KV 口径 Δ≤0.00121）**。
- **收尾例行（用户指令 2026-09-26）**：每次阶段性完成必须 ①更新 README.md 战役状态表 ②`docs/实验日志.md` 顶部加当日条目，随收尾 commit push。
- **T-perf 诊断已收官（步骤 022）**：①平步长三疑点（会话/路径/内容）全灭=boot 级方差；②溢出排除（Shared 488MB 平线+行为签名阴性）；③步内剖定罪 GPU 贴权重读地板 14.9ms/步、其余=图外 CPU 派发（spec 理论 2.5× 实拿 1.27×）；④**boot 方差根因= AOT 缓存校验尾换行非对称（`inspect.getsource` 补 `\n` vs `open().read()` 不补，parallel_state.py 无尾换行）→每 boot 重编译→生成码漂移；底座 `3498ef1` 修复后同产物两 boot ±0.4%**。历史「O(L) 斜率/平步长异常」重注=生成码方差产物；to_str 缓存（双轨）A/B 无净效应如实记。
- **归因修正存档（步骤 014）**：①「Marlin 分叉」**撤销**——0.27/0.29 内核清单与 Marlin 位宽表逐字相同，非迁移差异；「旧栈全线 Humming」是对步骤 010 单条日志的过度概括。②S6 的 eager 21.x vs 43 归因作废（口径混杂 + fp8/nvfp4 KV 读带宽差都可能贡献），批 4/5 后用 runner v2 口径重测。
- **批 3 免搬结论存档**：KV 域 4 文件 + humming 系 9 文件**零搬运**（0.29 已吸收全部 overlay 修复且部分更完善）——接手者勿再搬，改动清点的 +行数账本在「覆盖性判定」后仅是上界。
- **锚点缺口**：A7 vision 131k/155k 仍挂账（不阻塞）；A6/A8 已补采（`锚点数据包/smoke_s3_s6_20260926.json`）。
- **A5 115k 档 400**：错误体未捕获待查（不影响 100k 内锚）。
- **GSQ fp8 KV 实测单价**：42,810 B/token（GDN 状态同池）——GSQ 池值手算按此价。
- **批 3 侦察遗留尾巴（低优先）**：inc 域残差（inc_scheme 的 get_kvcache_method、inc_wna16_linear 的 INCXPUW4A16LinearScheme、inc_mxfp4_moe 37 行等）——XPU/KV 边角，不挡 GSQ 主线，批 5 后按需补。
- **回馈上游候选**：mapped_file Win32 移植、ops/utils 构建旗标修复 → SystemPanic/humming-windows PR。

## 三、资产地图（全路径）

| 资产 | 位置 | 说明 |
|---|---|---|
| 新底座 venv | `G:\qwen3.8model\vllm-win029` | cp312，vllm 0.29.0+cu132 / torch 2.11.0+cu130 / flashinfer 0.6.11.post3 / humming-kernels 0.1.15 |
| 底座 git 仓 | `G:\qwen3.8model\vllm-029base-git` | baseline `9948275` + `7bb4ce0`（embedding 补丁）；后续一切 0.29 侧改动在此提交 |
| 记录仓（本仓） | `G:\qwen3.8model\vllm-030win-git` → `github.com/deathwing475/vllm-030win` | 文档+工具+垫片+锚点；每步一 commit |
| 迁移源 | `G:\qwen3.8model\nvfp4-win-experiment\vllm-overlay` | tag `freeze-migration-20260925` 冻结；45 文件 +2,937/−252 |
| 旧生产 venv | `G:\qwen3.8model\vllm-win` | cp313 / 0.27.1 / torch 2.13——**冻结只读**，回退保全 |
| 锚点数据包 | `G:\qwen3.8model\vllm-030win-锚点数据包\`（副本在 docs/锚点数据包/） | PPL 双栈、长上下文矩阵、无草稿基线、共享显存轨迹 |
| humming 缓存 | `C:\fi\.humming\cache\` | device_info / nvrtc_compile / launcher 产物（HOME=C:\fi 是环境契约） |
| 垫片资产 | `tools/shims/`（SHIMS.md 索引） | 8 件第三方修复，重装环境必重放 |

## 四、关键数字速查

- **PPL 对锚**（0.27.1 nvfp4-KV → 0.29 bf16-KV）：8 条 |Δ|≤0.0025 ✅
- **⭐三线合并回归（步骤 018）**：线A 无 spec 56.12/55.97/54.32/50.38（4k/8k/32k/100k，对锚 +0.2~0.8%）；线B spec N=2 doc_4k median 73.00（锚 60.65）+ 8k 68.59/32k 41.45/100k 24.39（锚 57.49/34.12/18.86 = +19~29%）+ 接受率 0.72-0.80（锚带 0.68-0.80 内）；PPL 复锚 |Δ|≤0.00194 ✅；池账 no-spec 168,837 / spec 121,058 tokens @3.4e9
- **无草稿基线**：55.7 tok/s @4k/8k、54.2 @32k、50.3 @100k（步长 17.9→19.8ms 几乎平）= 用户基准 57-60 ✓
- **spec N=2**：57.5 @8k / 34.1 @32k / 18.9 @100k（步长 39→129ms，O(L) 归 spec 管线）
- **runner 原口径 N=3 doc_4k**：83.33 median（历史 85.18 复现，引擎零回退）
- **共享显存**：spec 组 422MB 常驻（vs 无草稿 106MB），decode 期平——溢出排除
- **A6 共存显存账（0.29 底座，无 spec）**：vllm 6,852 / BGE-M3 2,448 MiB，shared 94/82 负载零增长，卡级 9,368/16,303
- **GSQ fp8 KV 单价（0.29 实测）**：42,810 B/token（含 GDN 状态）；池 1,662,441,472 B = 38,836 tokens @32k 1.19×
- **批 3b S5 数据**：PIECEWISE AOT 编译 14.15s + 51 图捕获 56s（0.90 GiB）；PPL 复锚对批 3 |Δ|≤0.000821（`ppl_anchor_base029_batch3b.json`）；记账 59.5 tok/s @732p/256c 含 TTFT（eager 参考账 21.2-21.6）
- **0.29 纯底座 eager decode 参考账**：21.2-21.6 tok/s @4k/16k（vs 0.27.1 eager 43——头号嫌疑 = Marlin 接走 lm_head，批 3 归因）
- 阶段 0 锚全套见《锚点采集协议.md》A1-A5b 表；S3-S6 冒烟全量数据见 `锚点数据包/smoke_s3_s6_20260926.json`

## 五、下一步（顺序）

1. **三线合并回归（阶段 2 验收总账）**：spec 端到端（接受率 ±1pp + spec 吞吐对 A2/A4/A5 锚，生产配方 nvfp4 KV + DFlash2 draft）+ PIECEWISE 图模式 + 无 spec 吞吐，一次生产等价判定
2. 10-18 前完成硬承诺（生产等价+切换）；10-19 起 0.30 甄选滚动；10-30 止损线
