# 新会话提示词 — vLLM 0.30-on-Windows 迁移（2026-09-26 收档交接，阶段 2 五批全收官后）

> 用法：新会话开场把本文全文发给 AI。本文自包含。

---

你是 vLLM 0.30-on-Windows 迁移战役的执行者。先读这五样再动手（都在 `G:\qwen3.8model\vllm-030win-git\docs\`）：
①《交接文档.md》②《进度文档.md》③《实验日志.md》（每日流水）④《实验步骤文档.md》步骤 011-017（阶段 1 收官 + 批 1-5+3b）⑤《锚点采集协议.md》。有争议查三份 `vllm-030win-调研-*.md` 与《实验步骤文档.md》001-010。

## 使命

把现役 vLLM 0.27.1 Windows 自研栈（`vllm-overlay` 45 文件）迁移到 **SystemPanic vllm-win 0.29.0 底座**，甄选移植 0.30 功能，服务 **G:\qwen3.8model\Qwen3.8-27B-3Bit-GSQ**（compressed_tensors WNA16 int3 g128 + embed int4 g64）。**deadline 2026-10-30（新 27B 发布即止损）；硬承诺（生产等价+切换）10-18 前完成。**

## 当前状态（勿重复劳动）

**阶段 0/1 ✅ 收官；阶段 2 五批全收官（含批 3b）**：底座仓 `G:\qwen3.8model\vllm-029base-git` commit 链 `9948275→7bb4ce0(embedding)→57188ef(批1 协议/GC/hybrid)→f56c964(批2 TQ/KV)→7d4c0e1(批3 量化域)→5fe078a(批3b PIECEWISE custom-op)→76bec8b(批4 投机解码)→78db8a6(批5 nvfp4/SM120)`，每批已同步 venv `G:\qwen3.8model\vllm-win029`。

**⭐数值等价双口径锚全绿**：bf16-KV（`ppl_anchor_base029_batch4.json`，Δ≤0.0023）与 nvfp4-KV（`ppl_anchor_base029_batch5_nvfp4_r4.json`，A1 同口径 Δ≤0.00121）各 8/8。PIECEWISE 图模式已恢复（S5 PASS：51 图捕获全通）。SM120 nvfp4 全链确认：`decode_backend=xqa + kv uint8 + sm120` + E6.2 CUDA writer。

**覆盖性判定结论（勿重复搬运）**：批 2-5 合计免搬约 30 文件（KV 域 4 + humming 系 9 + 投机解码域 9/17 + utils/KV-spec 层/E6.1）；XQA 战略反转已存档：0.27 生产从未接 XQA spec，**批 5 已把 dedicated XQA 接上 nvfp4**（上游 assert 过严被修正）。

## 关键路径

新底座 venv `G:\qwen3.8model\vllm-win029`（cp312，**flashinfer 已升 0.6.18.post1**——生产同款纯 py 包整目录拷贝，旧版备份在 venv `flashinfer_0611_backup/`）｜底座 git 仓 `G:\qwen3.8model\vllm-029base-git`｜迁移源 `G:\qwen3.8model\nvfp4-win-experiment\vllm-overlay`（tag freeze-migration-20260925）｜0.30 树 `G:\qwen3.8model\vllm-0.30.0`｜旧生产 `G:\qwen3.8model\vllm-win`（冻结只读）｜锚点 `G:\qwen3.8model\vllm-030win-锚点数据包\`｜DFlash2 draft = `Qwen3.8-27B-3Bit-GSQ\dflash2\`（DFlash2DraftModel，自带 pack-quantized 配置）｜runner v2 = `nvfp4-win-experiment\_run_decode_bench.py`（`--cell N --wait-ready --runs 0` 等就绪 → `--cell N --warmup --runs 3`；prompt 族在 `Qwen3.8-27B-3Bit-GSQ\_spec_eval_prompts\`；默认 base 8080）｜记录仓=本仓（github deathwing475/vllm-030win）。

## 必守（违反必翻车）

1. **环境契约**：VS vcvars64 + HOME=C:\fi + CUDA 8.3 短路径 + LIB——抄 `tools/run_anchor_ppl_base029_batch5_nvfp4_r4.cmd`；humming 八件套垫片在 `tools/shims/`（垫片 #9 dynamo-disable 已试错撤销，勿再试）。
2. **⭐启动服务 cwd 铁律**：绝不能在记录仓 `vllm-030win-git` 目录下启动 python——一律 cd 到 `G:\qwen3.8model`。
3. **每步一 commit**：实验步骤文档（四段式）与改动同 commit；**每批收尾更新 README.md 状态表 + `docs/实验日志.md` 当日条目**（用户例行指令）再 push。**注意四段式插入标题时别吞下一步的标题行**（步骤 015/017 各踩过一次，写完 grep `^## 步骤` 复核序列）。
4. **搬运=覆盖性判定先行**：先 diff 三树（0.27 生产基 / overlay / 0.29 底座 / 0.30），确认 0.29 未覆盖才搬；「port from 0.30」类改动直接用 0.30 原生形态。调试遍历调用链不 grep 散点。
5. **测量纪律**：判等用 PPL/逐位；chunk≠token（按 usage.completion_tokens）；热身弃 1；禁 KV 溢出内存；**池值手动**（GSQ fp8 单价 **42,810 B/token**；nvfp4 锚 A1 原口径 gpu_mem=0.922，锚点复现跟随原口径）；**复跑锚点一律新文件名**；停服按命令行杀——**EngineCore spawn 孤儿用系统 Python 路径，Path 过滤必漏，必须按 CommandLine 匹配（spawn_main|vllm）全杀**；每轮清临时缓存查磁盘水位（保留 `_tmp_anchors`）。
6. **验收零容差**：性能总分 ≥ 现役；PPL 逐位；接受率 ±1pp；容量不缩水。性能回退先查口径（chunk/热身/合成 prompt 接受率）再谈回归。
7. **⭐flashinfer 0.6.18 两个新坑（runner 必设）**：`FLASHINFER_WORKSPACE_BASE=C:/fw`（**正斜杠**——0.6.18 把反斜杠 `\f` 吃成 form-feed 致 WinError 123）；`FLASHINFER_EXTRA_LDFLAGS=-LG:/qwen3.8model/vllm-win029/Lib/site-packages/tvm_ffi/lib -ltvm_ffi`（fp4 prefill JIT 链接必需，缺了 LNK2019）。
8. **已知缺口勿再排查**：XQA spec「接线」已完成（dedicated XQA 接 nvfp4，批 5）；humming×compile 已由批 3b custom-op 解；0.29 PIECEWISE=fullgraph AOT（dynamo-disable 是死路）；降级兜底的位等价验证必须**跨实现对照**（同函数调两次恒真——批 5 教训）。

## 下一步（按序执行）：三线合并回归 = 阶段 2 验收总账

三线一次跑完，一次生产等价判定（过关即达 10-18 硬承诺条件）。GPU 单卡串行，每线跑完杀净进程清缓存。

1. **线 A：无 spec 吞吐基线**（最简单，先跑）——`tools/serve_gsq_base029_s5.cmd` 去掉 PIECEWISE 改 eager 或直接用现配方改临时名；对照 A5b 锚：4k 55.7 / 8k 55.6 / 32k 54.2 / 100k 50.3 tok/s（runner v2 口径热身 1 + 3 重复中位）。注意现 serve 配方是 fp8 KV + 32k ctx——**长档（100k）需另配 nvfp4 KV + 长上下文 launcher**（照旧生产 `run_qwen_textonly_offload_200448_stable.cmd` 形态改 0.29 侧，池值手算）。
2. **线 B：spec 端到端**（主菜，批 4 欠账）——launcher = 生产配方衍生：GSQ + `--kv-cache-dtype nvfp4` + DFlash2 draft（`--speculative-config` 形态照旧生产 0.27 配方 `start_qwen3_8_nvfp4_long_5120.bat` / runner v2 历史配方，N=2）+ 池值手动。**验收对锚**：接受率 A4 锚 0.68-0.80（引擎 /metrics 口径）±1pp；吞吐 A5 锚 8k 57.5 / 32k 34.1 / 100k 18.9 tok/s；A2 口径 doc_4k N=2 median 60.65。启动失败按调用链遍历定罪（XQA spec 接线已就位，但 spec×nvfp4×PIECEWISE 组合第一次真跑）。
3. **线 C：PIECEWISE 图模式**（可与线 B 合并——spec + PIECEWISE 同配置跑即同时覆盖；单独再跑一次无 spec + PIECEWISE 对照 S5 的 59.5 tok/s 记账）。
4. 三线数据落 `锚点数据包/`（新文件名）；全过 ⇒ 更新 README/日志/进度，**阶段 2 正式收官**，进阶段 4（10-18 切换准备：生产 launcher 换血 + 回退预案）。不过线 ⇒ 按「归因 → 砍特性 → 重测 → 止损」兜底流程。
5. 挂账项（不阻塞回归）：A7 vision 131k/155k 锚、A5 115k 档 400 待查、批 3 侦察尾巴（inc XPU/KV 边角）、T-perf（spec 管线 O(L) 每步开销——回归数据出来后决定是否立项）。

开工前先看 `git log`（记录仓与底座仓各看一眼）确认仓库状态，从「下一步」第一条直接开干，不要重新调研已定事实。
