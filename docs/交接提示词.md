# 新会话提示词 — vLLM 0.30-on-Windows 迁移（2026-09-27 凌晨收档交接：图模式机制破解 + spec 新配置就绪待换装）

> 用法：新会话开场把本文全文发给 AI。本文自包含。

---

你是 vLLM 0.30-on-Windows 迁移战役的执行者。先读这五样再动手（都在 `G:\qwen3.8model\vllm-030win-git\docs\`）：
①《交接文档.md》②《进度文档.md》③《实验日志.md》（每日流水，条目一~十）④《实验步骤文档.md》步骤 018-026（三线回归→切换→R8→T-perf→快慢态定罪→图模式机制）⑤《锚点采集协议.md》。更早史（步骤 001-017、三份调研报告）有争议再查。

## 使命

把现役 vLLM 0.27.1 Windows 自研栈迁到 **SystemPanic vllm-win 0.29.0 底座**，甄选移植 0.30 功能，服务 **G:\qwen3.8model\Qwen3.8-27B-3Bit-GSQ**（compressed_tensors WNA16 int3 g128 + embed int4 g64，lm_head int3 走 GPTQ-marlin）。**deadline 2026-10-30（新 27B 发布即止损）；10-18 硬承诺（生产等价+切换）已于 09-26 提前 22 天收口。**

## 当前状态（勿重复劳动）

**✅ 生产运行在 0.29 栈**（步骤 020 切换 + 021 R8 修复）：入口 `G:\qwen3.8model\Qwen3.8-27B-3Bit-GSQ\run_dflash2_n2_base029.cmd`（shim→`tools/serve_gsq_prod029_n2.cmd`），配置=spec DFlash2 gptq3c N=2 + nvfp4 KV + 池 3.4e9 手动 + **PIECEWISE（待换装，见下一步①）** + prefix-cache + offload 8G + seqs 1；回退锚=旧 venv `vllm-win`（0.27.1 冻结只读）+ overlay tag `freeze-migration-20260925`。

**底座 commit 链**（`G:\qwen3.8model\vllm-029base-git`，每步同步 venv `vllm-win029`）：`9948275→…→78db8a6(批5)→6fc2108+e9d534d(回归两修复)→b315e77+1(offload Win 移植层)→d7cdb91(R8 驱动缺陷绕行)→3498ef1(AOT 缓存校验修复)→3cc6c64(草稿图化解耦)→8723e6c(GDN 包装减脂)`。记录仓=github deathwing475/vllm-030win（HEAD `e288a42`）。

**⭐图模式机制（步骤 026，最重要新知识）**：`--compilation-config {"cudagraph_mode":"PIECEWISE"}` **显式禁掉 FULL 图** ⇒ decode 图分段 replay + 每步 90+ splitting_ops（GDN 48/attention 21/kv_update 21）eager 派发；**不加 compilation-config 默认 FULL_AND_PIECEWISE** = prefill 走 PIECEWISE、decode 走 **FULL 整图一次 replay**（派发清零）⇒ 无 spec 18.0→14.2ms（−21%、GPU 占用 76→85%）、spec 27.0→25.9-26.4ms（3-boot 复测 −4%）。**DFlash2 在 FULL_AND_PIECEWISE 下已验证可用**（三段捕获全过：target PIECEWISE+FULL+草稿 FULL，无挂死、needle 32k 命中）——「spec 必须 PIECEWISE」历史约束已失效。新 launcher：`tools/serve_gsq_base029_nospec_nograph.cmd`（无 spec 版）、`tools/serve_gsq_base029_spec_nograph.cmd`（**换装蓝本**）、`tools/run_anchor_nospec.cmd`（0.27 对照，已按用户指定改 capture1/无 compilation-config）。

**⭐快慢态定罪（步骤 025）**：R 态（强制重编译 boot）20.9ms vs A 态（AOT 命中 boot）27.0ms，三口径（p50/mean/steady）为真非测量伪影；CPU 热身/睿频、SM/显存时钟、flashinfer autotune（本模型零命中，`choose_one` 仅 MoE/flashinfer-GEMM 调用）、并发抢带宽全部排除。载体已定名：①**marlin lm_head 双峰**——同核同 launch 配置（grid 70×256）每步交替 0.75ms vs 8-17ms（201:200 次），慢=eager 发射、快=图 replay 发射，零重叠零降频，**根因未结案**；②PIECEWISE 下 propose 链 flashinfer NVFP4 KV 写校验 `.cpu()` 每步 D2H 同步（`flashinfer/page.py _as_float32_scalar_tensors`）——缓存化补丁 A/B 判阴性已回退（E1 配置该路径在 FULL 图内消失）。

**测量教训（血泪，勿重蹈）**：单 boot 孤例不入账——E1 首测 20.06ms/119.7 tok/s 差点当 +32% 报喜，3-boot 复测判孤例（新 launcher 首启=重编译 boot 撞 R 态）；补丁 A/B 必须同协议多 boot（时段热态混淆：同补丁 26→32→35.6 递增漂移）。多 boot 复测协议见必守⑥。

## 关键路径

新底座 venv `G:\qwen3.8model\vllm-win029`（cp312，flashinfer 0.6.18.post1）｜底座 git 仓 `vllm-029base-git`｜迁移源 `nvfp4-win-experiment\vllm-overlay`（tag freeze-migration-20260925）｜0.30 树 `vllm-0.30.0`｜旧生产 `vllm-win`（冻结只读）｜锚点 `vllm-030win-锚点数据包\`｜生产日志 `G:\qwen3.8model\prod029_logs\`｜测量 scratch `G:\qwen3.8model\_tmp_line_b\`（boot_sample_runner/trace_pair/e1_3boot/bench_arms 系列 ps1 + boot_e2e.py + probe_*/find_*/analyze_* 分析器，均为步骤 025/026 实验工具）｜工具仓 `vllm-030win-git\tools\`（launcher 族 + anchor_longctx/anchor_ppl/probe_tperf_matrix）｜runner v2 = `nvfp4-win-experiment\_run_decode_bench.py`。

## 必守（违反必翻车；标 ⭐ 为新）

1. **环境契约**：VS vcvars64 + HOME=C:\fi + CUDA 8.3 短路径 + LIB CUDA lib\x64 + TMP/TEMP→G 盘（**勿删 `_tmp_anchors`**）——抄任一 serve_gsq launcher；humming 八件套垫片在 `tools/shims/`。
2. **启动服务 cwd 铁律**：绝不在记录仓 `vllm-030win-git` 目录下启 python（仓内 0.27 源码树会被 cwd 捕获），一律 cd 别处。
3. **每步一 commit**：步骤文档（四段式）与改动同 commit；收尾更新 README 状态表+实验日志当日条目再 push。**四段式插入时锚点会把下一个标题行吞掉（已踩三次！）——写完必 `grep -n "^## 步骤"` 复核序列**。
4. **搬运=覆盖性判定先行**（交接文档 §4）；调试遍历调用链不 grep 散点；未查清机制别硬讲结论；**机制假说必须实验收口**（用户曾叫停思考循环——不许空转刷屏）。
5. **测量纪律**：PPL/逐位判等；chunk≠token；热身弃 1+重复取中位；禁 KV 溢出内存（测量配置去 offload/prefix-cache）；池值手动；复跑锚点新文件名；停服按 CommandLine 匹配杀（spawn_main|vllm，**spawn 孤儿是系统 Python 路径、Path 过滤必漏**）；每轮清临时缓存查磁盘。
6. **⭐测量协议（步骤 025/026 强化）**：性能对比 **≥3 boot 复测取可复现水位，孤例数字不入账**（20.06 教训）；补丁/改动 A/B 必须同协议多 boot 对照（防时段热态混淆）；trace 分析器口径：`kernel`（小写）=GPU 内核 cat、步界用 gpu_user_annotation、**trace 时间单位是 µs（显示标签陷阱：打印成 us 会误判 1000 倍）**。
7. **⭐图模式知识**：显式 PIECEWISE=禁 FULL 图（每步 eager 派发）；无 compilation-config=FULL_AND_PIECEWISE（decode FULL 整图）。**FULL 图对 decode（batch≤3 验证安全）可用；prefill 的 nvfp4 reader 风险由 PIECEWISE 段隔离**。元数据分支/状态算子（GDN 系）入图会输出塌缩（实验 B 教训）——图改动必须过 needle 正确性门。捕获期 flashinfer `_as_float32_scalar_tensors` 拒绝非张量输入（`torch.tensor` 动态分配不能进捕获）。
8. **flashinfer 0.6.18 两个坑（runner/launcher 必设）**：`FLASHINFER_WORKSPACE_BASE=C:/fw`（**正斜杠**）；`FLASHINFER_EXTRA_LDFLAGS=-LG:/qwen3.8model/vllm-win029/Lib/site-packages/tvm_ffi/lib -ltvm_ffi`。page.py 校验缓存化补丁已回退（A/B 阴性），vendor=原状。
9. **Windows 坑**：①Python 注解 `"X" | str` 炸——整体写 `"X | str"`；②`inspect.getsource` 尾换行（坑七，`3498ef1` 已修）；③Bash 转义吃反斜杠——含 `\` 的替换写 python 脚本文件执行（`-c` 里转义翻车实录）；④`tools/*.cmd` 一律 CRLF（续行链内禁插注释行）；⑤链式 bash→PowerShell 偶发 exit 4294967295——分开跑；⑥flashinfer wheel 是 LF 行尾（打补丁别用 CRLF 锚）。
10. **验收零容差**：性能总分 ≥ 现役；PPL 逐位；接受率 ±1pp；容量不缩水；**新图配置必须 needle 命中+输出连贯**。性能回退先查口径三件套（chunk≠token/热身/接受率）再谈回归。

## 下一步（按序）

1. **⭐E1 配置长稳→换装生产（头名）**：`serve_gsq_base029_spec_nograph.cmd` 蓝本改 `tools/serve_gsq_prod029_n2.cmd`（删 compilation-config 行；prefix-cache/offload 8G 保留）；先真实请求长稳一轮（历史 1/20 低概率死锁教训：nvfp4+MTP 131k 死锁复现率随环境变化，runner v2 支持真实请求长稳）；过则换装+正确性门（needle/多轮 TTFT 21→3.1s 复验）+回退预案。预期 −4%。
2. **⭐快慢态机制继续（次优，潜在 ~24%）**：marlin lm_head 双峰结案（同核 0.75 vs 8-17ms、eager 慢/图 replay 快——先查两发射路径的 kernel 上下文差异）；R 态 20.9 与稳态 26 的运行时态差定罪；probe 工具链在 `_tmp_line_b/`。
3. **阶段 3（0.30 甄选滚动）**：A组+B7+D组+C1/C6/C8，砍尾 C8→C1/C6→D（清单见迁移计划 §2.2）。
4. **A7 vision 131k/155k 锚采证**（vision 面切换前置；vision 部署仍在 0.27）。
5. 挂账不阻塞：A5 115k 档 400、批 3 侦察尾巴。10-30 止损线。

开工前先 `git log`（记录仓+底座仓各一眼）确认状态，从「下一步」第一条直接开干，不要重新调研已定事实。
