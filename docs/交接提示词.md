# 新会话提示词 — vLLM 0.30-on-Windows 迁移（2026-09-26 深夜收档交接：生产已切换 + T-perf 优化战役累计 −14.4%）

> 用法：新会话开场把本文全文发给 AI。本文自包含。

---

你是 vLLM 0.30-on-Windows 迁移战役的执行者。先读这五样再动手（都在 `G:\qwen3.8model\vllm-030win-git\docs\`）：
①《交接文档.md》②《进度文档.md》③《实验日志.md》（每日流水，条目一~八）④《实验步骤文档.md》步骤 018-024（三线回归→切换→R8→T-perf 诊断→优化战役）⑤《锚点采集协议.md》。更早史（步骤 001-017、三份调研报告）有争议再查。

## 使命

把现役 vLLM 0.27.1 Windows 自研栈迁到 **SystemPanic vllm-win 0.29.0 底座**，甄选移植 0.30 功能，服务 **G:\qwen3.8model\Qwen3.8-27B-3Bit-GSQ**（compressed_tensors WNA16 int3 g128 + embed int4 g64）。**deadline 2026-10-30（新 27B 发布即止损）；10-18 硬承诺（生产等价+切换）已于 09-26 提前 22 天收口。**

## 当前状态（勿重复劳动）

**✅ 生产已运行在 0.29 栈**（步骤 020 切换 + 021 R8 offload 修复回填 `d7cdb91`）：入口 `G:\qwen3.8model\Qwen3.8-27B-3Bit-GSQ\run_dflash2_n2_base029.cmd`（shim→`tools/serve_gsq_prod029_n2.cmd`），配置=spec DFlash2 gptq3c N=2 + nvfp4 KV + 池 3.4e9 手动 + PIECEWISE + prefix-cache + offload 8G + seqs 1；多轮实证 32k 二轮 TTFT 21.0→3.1s；回退锚=旧 venv `vllm-win`（0.27.1 冻结只读）+ overlay tag `freeze-migration-20260925`。

**底座 commit 链**（`G:\qwen3.8model\vllm-029base-git`，每步同步 venv `vllm-win029`）：`9948275→…→78db8a6(批5)→6fc2108+e9d534d(回归两修复)→b315e77+1(offload Win 移植层)→d7cdb91(R8 cuMemcpyBatchAsync 驱动缺陷绕行)→3498ef1(AOT 缓存校验修复)→3cc6c64(草稿图化解耦)→8723e6c(GDN 包装减脂)`。

**⭐T-perf 战果（勿重测）**：decode 步长 **31.53→27.0ms（−14.4%）、吞吐 77→90.5 tok/s**，正确性（needle/chat）与接受率（0.68-0.74 带内）全保持。构成：①**boot 方差根因修复**（`3498ef1`：AOT 缓存校验 `inspect.getsource` 补尾换行 vs `open().read()` 不补→每 boot 重编译→生成码漂移；修复后同产物复测 ±0.4%）②**草稿图化解耦**（`3cc6c64`：`dflash/speculator.py` 把草稿图模式绑死在 target 模式导致 PIECEWISE 下草稿全 eager，~3.7ms/步胶水；解耦给草稿 FULL_DECODE_ONLY 图）③**GDN 包装减脂五处**（`8723e6c`：静态合取缓存/尺寸视图缓存/消重复 metadata 解析）。历史「O(L) 0.71µs/token」「stage0 平步长异常」已重注=boot 级生成码方差产物；溢出嫌疑已仪器排除（Shared 488MB 平线）；GPU 内核并集 14.9ms/步=权重读取地板（27B 3bit ~12.5GB/960GB/s）。

**⭐快慢态孤例=现挂账头名（最值钱）**：boot-1 实测 **20.85ms** 但同产物 boot-2/3=26.97/27.08 未复现（同代码同编译产物 ⇒ 机制在运行时层：候选=GPU/CPU 争用瞬时态、flashinfer 运行时 autotune 选型噪声〔其缓存每次「Saved 0 configs」从不持久〕、CPU 睿频/核驻留）。**捕获=再降 ~24%**。历史快态目击 25.78/26.79 同族。稳态口径=27.0±0.1（三 boot 采样）。

## 关键路径

新底座 venv `G:\qwen3.8model\vllm-win029`（cp312，flashinfer 0.6.18.post1，旧版备份 venv `flashinfer_0611_backup/`）｜底座 git 仓 `vllm-029base-git`｜迁移源 `nvfp4-win-experiment\vllm-overlay`（tag freeze-migration-20260925）｜0.30 树 `vllm-0.30.0`｜旧生产 `vllm-win`（冻结只读）｜锚点 `vllm-030win-锚点数据包\`｜生产日志 `G:\qwen3.8model\prod029_logs\serve_prod029.log`｜测量 scratch `G:\qwen3.8model\_tmp_line_b\`（probe_tperf_matrix/e2e_short_decode/analyze_trace2 等工具脚本）｜工具仓 `vllm-030win-git\tools\`（probe_tperf_matrix.py、anchor_longctx.py、anchor_ppl.py、serve_gsq_* launcher 族）｜runner v2 = `nvfp4-win-experiment\_run_decode_bench.py`｜记录仓=github deathwing475/vllm-030win。

## 必守（违反必翻车；新增三条标 ⭐）

1. **环境契约**：VS vcvars64 + HOME=C:\fi + CUDA 8.3 短路径 + LIB CUDA lib\x64 + TMP/TEMP→G 盘（**勿删 `_tmp_anchors`**）——抄 `tools/run_anchor_ppl_base029_batch5_nvfp4_r4.cmd` 或任一 serve_gsq launcher；humming 八件套垫片在 `tools/shims/`。
2. **启动服务 cwd 铁律**：绝不在记录仓 `vllm-030win-git` 目录下启 python（仓内 0.27 源码树会被 cwd 捕获），一律 cd 别处。
3. **每步一 commit**：步骤文档（四段式）与改动同 commit；收尾更新 README 状态表+实验日志当日条目再 push。**⭐四段式插入时锚点会把下一个标题行吞掉（已踩三次！）——写完必 `grep -n "^## 步骤"` 复核序列**。
4. **搬运=覆盖性判定先行**（见交接文档 §4，批 2-5 合计免搬约 30 文件，勿重复）；调试遍历调用链不 grep 散点；未查清机制别硬讲结论。
5. **测量纪律**：PPL/逐位判等；chunk≠token（usage.completion_tokens）；热身弃 1+重复取中位；禁 KV 溢出内存（测量配置去 offload/prefix-cache）；池值手动；复跑锚点新文件名；停服按 CommandLine 匹配杀（spawn_main|vllm，**spawn 孤儿是系统 Python 路径、Path 过滤必漏**）；每轮清临时缓存查磁盘。
6. **⭐测量协议（T-perf 战役新增）**：性能对比必须 **AOT 命中 boot 对 AOT 命中 boot**（日志看 `Directly load AOT compilation`×3 vs `Compiling model again`——每 boot 重编译会重掷生成码、方差可达 ±16%）；**同产物跨 boot 复测，孤例数字不入账**（20.85 教训）；trace 分析器口径：`kernel`（小写）才是 GPU 内核 cat、步界用 gpu_user_annotation、窗口单位是 µs。
7. **⭐图化纪律**：无状态算子（humming GEMM）可进 CUDA 图；**元数据分支/状态算子（GDN 系）入图会输出塌缩**（实验 B 32k 重复风暴）——入图实验必须过 needle 输出正确性门。草稿错了只掉接受率（verify 兜底），target 算子错了直接坏输出——风险不对称，先打风险低的。
8. **flashinfer 0.6.18 两个坑（runner/launcher 必设）**：`FLASHINFER_WORKSPACE_BASE=C:/fw`（**正斜杠**，`\f` 被吃成 form-feed）；`FLASHINFER_EXTRA_LDFLAGS=-LG:/qwen3.8model/vllm-win029/Lib/site-packages/tvm_ffi/lib -ltvm_ffi`。
9. **Windows 新坑四条**：①Python 注解 `"X" | str` 在 def 时求值炸——整体写 `"X | str"`；②`inspect.getsource` 给无尾换行文件补 `\n`（AOT 缓存校验永失效根因，坑七）；③Bash heredoc 折叠 `\\`、`$_` 被吞（PowerShell 代码走 .ps1 文件）；④`tools/*.cmd` 一律 CRLF。
10. **验收零容差**：性能总分 ≥ 现役；PPL 逐位；接受率 ±1pp；容量不缩水。性能回退先查口径三件套（chunk≠token/热身/接受率）再谈回归。

## 下一步（按序）

1. **⭐快慢态孤例定罪（头名，潜在再降 24%）**：反复重启采样态分布（≥6 boot，每次 e2e 三连+记录 nvidia-smi 时钟/功耗/CPU 争用）；查 flashinfer 运行时 autotune 选型（每次「Saved 0 configs」从不持久——若选型有噪声，设法持久化或钉死配置）；对照 20.85 快态会话与 27.0 稳态会话的系统状态差异。
2. **CPU 派发优化继续**（若快慢态无果再打）：attention 包装（226µs×21/步）、unified_kv_cache_update（329µs×21/步）、apply_humming 派发单价（~95µs/次，草稿每步 6 次）。
3. **阶段 3（0.30 甄选滚动）**：A组+B7+D组+C1/C6/C8，砍尾 C8→C1/C6→D（清单见迁移计划 §2.2）。
4. **A7 vision 131k/155k 锚采证**（vision 面切换前置；vision 部署仍在 0.27）。
5. 挂账不阻塞：A5 115k 档 400、批 3 侦察尾巴。10-30 止损线。

开工前先 `git log`（记录仓+底座仓各一眼）确认状态，从「下一步」第一条直接开干，不要重新调研已定事实。
