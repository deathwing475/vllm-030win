# 新会话提示词 — vLLM 0.30-on-Windows 迁移（2026-09-26 收档交接，批 1-3 后）

> 用法：新会话开场把本文全文发给 AI。本文自包含。

---

你是 vLLM 0.30-on-Windows 迁移战役的执行者。先读这五样再动手（都在 `G:\qwen3.8model\vllm-030win-git\docs\`）：
①《交接文档.md》②《进度文档.md》③《实验日志.md》（每日流水）④《实验步骤文档.md》步骤 011-014（阶段 1 收官 + 批 1-3）⑤《锚点采集协议.md》。有争议查三份 `vllm-030win-调研-*.md` 与《实验步骤文档.md》001-010。

## 使命

把现役 vLLM 0.27.1 Windows 自研栈（`vllm-overlay` 45 文件）迁移到 **SystemPanic vllm-win 0.29.0 底座**，甄选移植 0.30 功能，服务 **G:\qwen3.8model\Qwen3.8-27B-3Bit-GSQ**（compressed_tensors WNA16 int3 g128 + embed int4 g64）。**deadline 2026-10-30（新 27B 发布即止损）；硬承诺（生产等价+切换）10-18 前完成。**

## 当前状态（勿重复劳动）

阶段 0/1 ✅ 收官（S1-S6 全过、A6/A8 锚已补采）；**阶段 2 五批已落三批**：批1 协议/GC/hybrid（`57188ef`）、批2 TQ+KV（`f56c964`）、批3 量化域（`7d4c0e1`，**PPL 重锚 8/8 全绿**）。底座仓 `G:\qwen3.8model\vllm-029base-git` commit 链 `9948275→7bb4ce0→57188ef→f56c964→7d4c0e1`，每批已同步 venv `G:\qwen3.8model\vllm-win029`。

**⭐覆盖性判定结论（勿重复搬运）**：KV 域 4 文件 + humming 系 9 文件**零搬运**——0.29 已吸收全部 overlay 等价物且部分更完善；改动清点行数只是上界，**每文件先做覆盖性判定再动手**。两项归因修正已存档：「Marlin 分叉」撤销（0.27/0.29 逐字相同，非迁移差异）；S6 的 eager 21.x vs 43 归因作废（口径混杂），后续用 runner v2 口径重测。

## 关键路径

新底座 venv `G:\qwen3.8model\vllm-win029`（cp312）｜底座 git 仓 `G:\qwen3.8model\vllm-029base-git`｜迁移源 `G:\qwen3.8model\nvfp4-win-experiment\vllm-overlay`（tag freeze-migration-20260925）｜0.30 树 `G:\qwen3.8model\vllm-0.30.0`｜旧生产 `G:\qwen3.8model\vllm-win`（冻结只读）｜锚点 `G:\qwen3.8model\vllm-030win-锚点数据包\`｜记录仓=本仓（github deathwing475/vllm-030win）。

## 必守（违反必翻车）

1. **环境契约**：VS vcvars64 + HOME=C:\fi + CUDA 8.3 短路径 + LIB——直接抄 `tools/run_anchor_ppl_base029.cmd`；humming 八件套垫片在 `tools/shims/`（SHIMS.md 索引；垫片 #9 dynamo-disable **已试错撤销**，勿再试）。
2. **⭐启动服务 cwd 铁律**：绝不能在记录仓 `vllm-030win-git` 目录下启动 python（仓内 0.27.1 源码树会被捕获，炸 `vllm._C_stable_libtorch`）——一律 cd 到 `G:\qwen3.8model`。
3. **每步一 commit**：实验步骤文档（四段式）与改动同 commit；**每批收尾更新 README.md 状态表 + `docs/实验日志.md` 当日条目**（用户例行指令）再 push。
4. **搬运=覆盖性判定先行**：先 diff 三树（0.27 生产基 / overlay / 0.29 底座 / 0.30），确认 0.29 未覆盖才搬；「port from 0.30」类改动直接用 0.30 原生形态（收敛）。调试遍历调用链不 grep 散点。
5. **测量纪律**：判等用 PPL/逐位；chunk≠token；热身弃 1；禁 KV 溢出内存；池值手动（GSQ fp8 单价 **42,810 B/token**）；**复跑锚点一律新文件名**（--out 曾覆盖主锚）；停服按命令行杀（含 spawn 孤儿+zmq 29550）；每轮清临时缓存查磁盘水位（**保留 `_tmp_anchors` 目录**——PPL runner 的 TMP，删了 LNK1104）。
6. **验收零容差**：性能总分 ≥ 现役；PPL 逐位；接受率 ±1pp；容量不缩水。性能回退先查口径（chunk/热身/合成 prompt 接受率）再谈回归。
7. CRLF：写 .cmd 必须 CRLF；**cmd `^` 续行链内禁插注释行**（rem 被当参数/行尾 `^` 吞真参数），rem 掉带 `^` 的行必须连 `^` 删；python 读写先归一化 `\r\n`。
8. **已知缺口勿再排查**：纯底座无 SM120 nvfp4-KV（批 5）；SM120 静态 XQA 且上游 XQA spec 未接线 ⇒ 一切投机解码纯底座不可用（批 4+5 一起解）；0.29 PIECEWISE=fullgraph AOT（PIECEWISE 恢复 = 批 3b custom-op 方案，dynamo-disable 是死路）。

## 下一步（按序执行）

1. **批 3b（半小时级）**：vllm 侧把 `apply_humming_linear`（`vllm/model_executor/layers/quantization/utils/humming_utils.py:549`）注册为 torch custom op（`torch.library.custom_op` + `register_fake` 推 shape；LayerConfig 经 `config.to_str()` 字符串化过 op 边界）→ 用 `tools/serve_gsq_base029.cmd` 改临时 PIECEWISE 配置启动复测（S5 判定=启动成功+功能请求过；吞吐记账不设卡）。
2. **批 4 投机解码**：DFlash2 全套（5 文件：qwen3_dflash2.py、dflash2/{__init__,speculator}.py、registry/config 接线）+ DFlash v1（6 文件）+ 支撑件（6 文件：llm_base_proposer/caching/gumbel/logits_processor/mtp/…）；同批解 XQA spec 接线；验收 = PPL + 接受率对锚（±1pp）+ spec 吞吐。
3. **批 5 nvfp4/SM120**：flashinfer.py +137 自研行逐 hunk 重放 + reference_nvfp4 直搬 + utils/flashinfer.py；验收 = nvfp4 KV 启动 + PPL + A7 vision 锚补采（顺带）。
4. 每批收尾按例行更新 README + 实验日志；10-18 切换；10-19 起 0.30 甄选滚动；10-30 止损。

开工前先看 `git log`（记录仓与底座仓各看一眼）确认仓库状态，从「下一步」第一条直接开干，不要重新调研已定事实。
