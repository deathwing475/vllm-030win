# 新会话提示词 — vLLM 0.30-on-Windows 迁移（2026-09-26 收档交接）

> 用法：新会话开场把本文全文发给 AI。本文自包含。

---

你是 vLLM 0.30-on-Windows 迁移战役的执行者。先读这五样再动手（都在 `G:\qwen3.8model\vllm-030win-git\docs\`）：
①《交接文档.md》②《进度文档.md》③《vllm-030win-迁移计划.md》④《实验步骤文档.md》步骤 001-010 ⑤《锚点采集协议.md》。有争议查三份 `vllm-030win-调研-*.md`。

## 使命

把现役 vLLM 0.27.1 Windows 自研栈（`vllm-overlay`，45 文件 +2,937 行：DFlash2 投机解码、INC/humming 量化、nvfp4-KV SM120、TQ KV、KV 异构池等）迁移到 **SystemPanic vllm-win 0.29.0 底座**，甄选移植 0.30 功能（A组+B7+D组+C1/C6/C8），服务 **G:\qwen3.8model\Qwen3.8-27B-3Bit-GSQ**（compressed_tensors WNA16 int3 g128 + embed int4 g64，model_type qwen3_5）。**deadline 2026-10-30（下一个 27B 模型发布即止损）；硬承诺（生产等价+切换）10-18 前完成。**

## 当前状态（勿重复劳动）

阶段 0 ✅ 收官；阶段 1 **关键判定双过**（S1 GSQ int3 裸加载 ✅、S2 PPL 对锚 |Δ|≤0.0025 ✅）；剩 S3-S6 冒烟（MiniCPM 共存/tool-call/PIECEWISE/JIT）+ A6-A8 锚点补采，然后进阶段 2 五批迁移（批表在计划 §2.2）。T-perf 优化任务留到移植后（证据包在步骤 006-008，靶子=spec 步长 2.2×→1.4×）。

## 关键路径

新底座 venv `G:\qwen3.8model\vllm-win029`（cp312）｜底座 git 仓 `G:\qwen3.8model\vllm-029base-git`（baseline 9948275+7bb4ce0）｜迁移源 `G:\qwen3.8model\nvfp4-win-experiment\vllm-overlay`（tag freeze-migration-20260925）｜旧生产 `G:\qwen3.8model\vllm-win`（冻结只读）｜锚点 `G:\qwen3.8model\vllm-030win-锚点数据包\`｜记录仓=本仓（github deathwing475/vllm-030win）。

## 必守（违反必翻车）

1. **环境契约**：跑 vllm 必须 VS vcvars64 + HOME=C:\fi + CUDA_HOME 8.3 短路径反斜杠 + LIB 含 CUDA/Python libs——**直接抄 `tools/run_anchor_ppl_base029.cmd`**；humming 八件套垫片在 `tools/shims/`（重装必重放，humming-windows 0.1.15 并未真正移植 Windows）。
2. **每步一 commit**：实验步骤文档（协议/操作/结果/判定四段式）与改动同 commit，每阶段 push；底座改动进 vllm-029base-git。
3. **搬运=逐行读完再动手**（同名≠语义同），改名 grep 全仓；调试遍历调用链不 grep 散点。
4. **测量纪律**：判等用 PPL/逐位不用文本；chunk≠token（按 usage.completion_tokens）；热身弃 1+重复取中位；**禁 KV 溢出内存**（测量配置去 offload/prefix-cache）；池值手动设 `--kv-cache-memory-bytes`；停服按命令行杀（含 spawn 孤儿+zmq 29550 端口）；每轮清临时缓存查磁盘水位。
5. **验收零容差**：性能总分 ≥ 现役（兜底=特性级取舍，砍完仍不达标即止损）；PPL 逐位；接受率 ±1pp；容量不缩水。
6. **已知缺口勿再排查**：0.29 纯底座无 SM120 nvfp4-KV（批 5 自研补丁搬完才有）；`qwen3_5.py` embedding 2 行已打（7bb4ce0）；A5 115k 档 400 待查小尾巴。
7. CRLF：写 .cmd 必须 CRLF（python 读写会毁行尾——raw 重写）；路径字符串用 raw（`\N` 转义炸过）。

## 下一步（按序执行）

1. S3 冒烟（MiniCPM5-2B+DSpark+BGE-M3 共存）+ A6 显存账锚 → S4/A8 tool-call → S5 PIECEWISE → S6 长稳；跑完阶段 1 收官。
2. 阶段 2 批 1（协议/GC/hybrid，三方合并含 0.30 同文件 hunk）……批 5（nvfp4/SM120，含 flashinfer.py +137 自研逐 hunk 重放）。
3. 每批验收按锚点协议；10-18 切换；10-19 起 0.30 甄选滚动（砍尾 C8→C1/C6→D）；10-30 止损。

开工前先看 `git log` 确认仓库状态，从"下一步"第一条直接开干，不要重新调研已定事实。
