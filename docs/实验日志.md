# 实验日志（项目日志，按日倒序）

> 与《实验步骤文档.md》的分工：步骤文档 = 单个实验的四段式记录（协议/操作/结果/判定）；
> 本日志 = 按日的工作流水（做了什么、关键结论、踩坑、下一步）。每次阶段性收尾更新。

---

## 2026-09-26 — 阶段 1 收官 + 阶段 2 批 1-3 落地

**做了什么**（接手交接提示词后单日完成）：

1. **阶段 1 冒烟收官（步骤 011）**：S3a MiniCPM5-2B+BGE-M3 共存冒烟 + A6 显存账锚（Shared 94/82 MiB 负载零增长，无溢出）；S4/A8 GSQ tool-call+reasoning（fp8 KV 跑通，实测单价 42,810 B/token）；S5 PIECEWISE 定性；S6 长稳 20/20。dspark/XQA 投机缺口挂批 4。
2. **批 1 协议/GC/hybrid（步骤 012，底座 `57188ef`）**：gc freeze 用 0.30 收敛形态接线；协议 ≥128k 输出预留 1 token（三态验证）；hybrid 实验开关。
3. **批 2 KV/TQ（步骤 013，底座 `f56c964`）**：multi_turboquant_kv 直搬 + triton_attn 四点接线 + pip 包入 venv；**KV 域 4 文件零搬运**（0.29 已吸收全部 overlay 修复——GCD hash/降级/prefix_cacheable/INT4 折半）。
4. **批 3 量化域（步骤 014，底座 `7d4c0e1`）**：humming 系 9 文件零搬运（0.29 严格更新）；inc 域合并（calib/inc_embedding 直搬 + 低 bit humming 派发 + 码本 embedding 分支 + 5/6/7-bit）；torch_wna16 直搬注册清单尾；**PPL 重锚 8/8 全绿（最大 Δ +0.00068）**。

**关键结论**：

- ⭐方法论：**逐 hunk 覆盖性判定先于搬运**——0.29（SystemPanic）不是 0.27.1 的等价物，它吸收了大量上游演进；批 2/3 合计免搬约 20 文件。
- ⭐归因修正：「Marlin 分叉」撤销——0.27/0.29 内核清单与 Marlin 位宽表逐字相同，非迁移差异；「旧栈全线 Humming」是对步骤 010 单条日志的过度概括；S6 的 eager 21.x vs 43 归因作废（口径混杂 + fp8/nvfp4 KV 读带宽差都可能贡献），后续用 runner v2 口径重测。

**踩坑**：

- cmd `^` 续行链内不能插注释行（一场连踩三次：rem 被当参数 / 注释行尾 `^` 吞掉真参数 / 再次误插）；注释只能整块放命令前，rem 掉带 `^` 的行必须连 `^` 删。
- 在记录仓目录下启动 python 会捕获仓内 0.27.1 源码树（炸 `vllm._C_stable_libtorch`）——启动一律 cd 到 `G:\qwen3.8model`。
- 复跑锚点 `--out` 指向主锚文件会覆盖它（docs 副本救回）——复跑一律用新文件名。
- 删 `_tmp_anchors` 目录导致 LINK LNK1104（它是 PPL runner 的 TMP 重定向目标）——保留。
- PIECEWISE×humming：`torch._dynamo.disable` 垫片在 eager 无害但 fullgraph AOT 下是硬错误（gb0098）——已撤销，正确方案 = `apply_humming_linear` 注册 custom op（挂批 3b）。

**下一步**：批 3b（custom-op，恢复 PIECEWISE）→ 批 4（投机解码：DFlash2 全套 + XQA 缺口）→ 批 5（nvfp4/SM120，flashinfer.py +137 行逐 hunk 重放）→ 阶段 4 回归切换（10-18 硬承诺）。

---
