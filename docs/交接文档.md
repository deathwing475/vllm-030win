# vLLM 0.30-on-Windows 迁移 — 交接文档

> 2026-09-26 收档（步骤 017，阶段 2 五批全收官）。接手必读顺序：本文 → 《进度文档.md》→ 《实验日志.md》（每日流水）→ 《vllm-030win-迁移计划.md》→ 《实验步骤文档.md》（001-017）→ 三份调研报告（有争议时查数据出处）。

## 1. 一句话现状

**阶段 0/1/2 全收官（2026-09-26 步骤 018：三线合并回归全过，生产等价判定成立）**：底座仓 commit 链 `9948275 → 7bb4ce0 → 57188ef(批1) → f56c964(批2) → 7d4c0e1(批3) → 5fe078a(批3b) → 76bec8b(批4) → 78db8a6(批5) → 6fc2108 + e9d534d(回归两修复)`，每步已同步 venv。三线合并回归：线A 无 spec 四档超锚、线B spec 端到端 A2/A4/A5 三锚全过（吞吐 +19~29%、接受率带内偏上）、线C 图模式合并覆盖、PPL 复锚 8/8 带内。**下一步=阶段 4（10-18 切换：生产 launcher 换血 + 回退预案）**。deadline 2026-10-30。

## 2. 环境契约（跑任何 vllm 前置条件，缺一必炸）

1. **VS 环境**：`call "C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\VC\Auxiliary\Build\vcvars64.bat"`（torch/humming 现场编译要 cl+ninja）
2. **HOME/USERPROFILE=C:\fi**（humming 缓存键的一部分——不一致触发重建然后炸）
3. **CUDA_HOME/CUDA_PATH=C:\PROGRA~1\NVIDIA~2\CUDA\v13.3**（8.3 短路径反斜杠）
4. **LIB 加 CUDA lib\x64**；TMP/TEMP 重定向 G 盘（**勿删 `G:\qwen3.8model\_tmp_anchors`**——PPL runner 的 TMP 目录，删了 LINK LNK1104）
5. zmq 29550 端口孤儿先清（launcher 已内置）
6. **完整可跑模板** = `tools/run_anchor_ppl_base029.cmd`（PPL）/ `tools/serve_gsq_base029.cmd`（GSQ eager + fp8 KV + tool-call parsers）/ `tools/serve_minicpm5_2b_dspark_base029.cmd`（MiniCPM5 共存，dspark 注释带原因）
7. **⭐启动服务 cwd 铁律**：绝不能在 `vllm-030win-git` 记录仓目录下启动 python——仓内躺着 0.27.1 源码树（`vllm/`），cwd 会捕获它（炸 `vllm._C_stable_libtorch`，极像环境坏）。一律 cd 到 `G:\qwen3.8model`。

## 3. 已修复/已建（重装环境必重放）

- **humming-windows 0.1.15 八件套**（`tools/shims/SHIMS.md`）：dlfcn/mman/GNU 旗标/.so 命名等的 Windows 移植。垫片 #9（dynamo-disable）**已试错撤销**——fullgraph AOT 下 disable 函数硬错误（gb0098），SHIMS.md 有正确方案记录。
- **qwen3_5.py 量化 embedding 2 行**（底座 `7bb4ce0`）
- **批 1-3 落地物**（底座仓，全部已同步 venv）：gc freeze 接线、协议 ≥128k 预留 1 token、hybrid 开关、multi_turboquant_kv 直搬+triton_attn 四点接线（pip 包 multi_turboquant 0.1.0 已装）、inc 域合并（calib/inc_embedding 直搬、低 bit humming 派发、码本 embedding 分支、5/6/7-bit）、torch_wna16 注册清单尾
- **multi_turboquant 0.1.0**：venv 已装（生产同款 dist whl：`G:\qwen3.8model\vllm-win\dist-v0.27.1\`）
- **批 3b（`5fe078a`）**：`vllm::apply_humming_linear` custom op（humming_utils.py）——LayerConfig 经 to_str 过 op 边界、op 内 lru_cache 重建（frozen 安全）、register_fake 复用 humming meta 函数；6 调用点零改动。PIECEWISE 启动配方 = `tools/serve_gsq_base029_s5.cmd`（JSON 参数 `\"` 转义经 argv 探测实证）

## 4. ⭐覆盖性判定结论（勿重复搬运！）

**0.29（SystemPanic）不是 0.27.1 的等价物**——它吸收了大量上游演进。以下 overlay 内容**已判定免搬**：
- **KV 域 4 文件**（kv_cache_utils/coordinator/kv_cache_interface/attention）：mamba 非 align 守卫、GCD hash（取代 LCM）、partial-hit 降级、prefix_cacheable、INT4 页折半——0.29 全有等价物
- **humming 系 9 文件**：0.29 = LayerConfig API 形态，严格新于 overlay（MoE 配置链/gemm1 参数/uint5-7 映射/has_g_idx 守卫全在）
- 改动清点的 +行数账本只是上界；**每文件先做覆盖性判定再动手**。

## 5. 归因修正存档（防重蹈）

1. **「Marlin 分叉」撤销**：0.27/0.29 的内核清单顺序与 Marlin 位宽表（uint4b8/uint8b128/fp8/fp4）逐字相同——0.27 生产同样由 Marlin 接 int4 embed/lm_head，**非迁移差异**；「旧栈全线 Humming」是对步骤 010 单条日志的过度概括。
2. **S6 的 eager 21.x vs 43 归因作废**：口径混杂（单发续写 vs runner 3×5 median）+ fp8/nvfp4 KV 读带宽差都可能贡献；批 4/5 后用 runner v2 口径重测再谈。
3. 0.29 的 reasoning 字段名 = `message.reasoning`（非 reasoning_content）；GSQ chat_template 硬编码 "Reasoning effort xhigh" system 提示——thinking 发散/答非所问归模型+模板，非引擎（S2/S14 PPL 已锁前向等价）。

## 6. 未决/在途

1. **阶段 4 切换（10-18 硬承诺收口）**：生产 launcher 换血（0.29 venv + `tools/serve_gsq_base029_lineB_spec.cmd` 配方衍生）+ 回退预案（旧 venv `vllm-win` 冻结只读）+ 切换演练。回归数据=步骤 018 / `锚点数据包/lineA_*`、`lineB_*`。
2. **T-perf**（待立项，素材已更新步骤 018）：spec 管线 O(L) 步长斜率 0.98→0.71µs/token（100k 132→98ms，仍存）；异常=stage0 真实请求口径 0.29 上步长全程平（31-33ms vs 同栈 A5 口径 50-98ms），立项先分离会话/请求路径/内容。
3. **锚点缺口**：A7 vision 131k/155k 挂账；A5 115k 档 400 待查（不影响 100k 内锚）。
4. **批 3b 顺带记录**：PIECEWISE 下 AOT 编译 14.15s + 51 图捕获 56s（0.90 GiB）；eager → 图模式粗口径对比 59.5 vs 21.2-21.6 tok/s（含 TTFT，正式对比在回归用 runner v2 口径）。
5. **停服杀进程**：EngineCore spawn 孤儿用**系统 Python** 路径（非 venv Scripts），Path 过滤必漏——必须按 CommandLine 匹配（spawn_main/vllm）全杀（批 4 PPL 首跑 LNK1104 重演教训㉑）。
6. **⭐flashinfer 已升 0.6.18.post1（venv 级，重装环境必重放）**：0.29 venv 从冻结生产 venv 整目录拷贝 flashinfer（纯 py JIT 包）；runner 必设 `FLASHINFER_WORKSPACE_BASE=C:/fw`（**正斜杠**——0.6.18 把反斜杠 `\f` 吃成 form-feed 致 WinError 123）+ `FLASHINFER_EXTRA_LDFLAGS=-LG:/qwen3.8model/vllm-win029/Lib/site-packages/tvm_ffi/lib -ltvm_ffi`（fp4 prefill JIT 链接必需，缺了 LNK2019）。旧 0.6.11 备份在 venv `flashinfer_0611_backup/`。

## 7. 测量纪律（不变项，违反必翻车）

判等用 PPL/逐位不用文本；chunk≠token（按 usage.completion_tokens）；热身弃 1+重复取中位；禁 KV 溢出内存（测量配置去 offload/prefix-cache）；池值手动 `--kv-cache-memory-bytes`（GSQ fp8 单价 **42,810 B/token** 手算按此）；停服按命令行杀（含 spawn 孤儿+zmq 29550）；**复跑锚点一律新文件名**（--out 覆盖过主锚一次，docs 副本救回）；每轮清临时缓存查磁盘水位（保留 _tmp_anchors）。

## 8. 仓库纪律

**每步一 commit（四段式）+ 每批收尾更新 README 状态表 + 实验日志当日条目**（用户指令 2026-09-26），每阶段 push → `deathwing475/vllm-030win`；底座改动进 `vllm-029base-git`（改动必须走 git）；不传权重/whl/.pyd/大 log。CRLF：写 .cmd 必须 CRLF（**续行链内禁插注释行**，rem 掉带 `^` 的行必须连 `^` 删——一场连踩三次）；python 读写先归一化 `\r\n`。
