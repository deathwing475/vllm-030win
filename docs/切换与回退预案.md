# 生产切换与回退预案（阶段 4，2026-09-26）

> 0.29 栈生产等价判定已成立（步骤 018 三线合并回归全过）。本文是切换操作手册 + 演练记录。旧栈（0.27.1）冻结只读保留为回退锚。

## 1. 生产 launcher（0.29 栈）

| 项 | 值 |
|---|---|
| 真源（git 管理） | `tools/serve_gsq_prod029_n2.cmd` |
| 部署入口 | `G:\qwen3.8model\Qwen3.8-27B-3Bit-GSQ\run_dflash2_n2_base029.cmd`（3 行 shim，call 真源） |
| 引擎 | vLLM 0.29.0（底座 `9948275→…→78db8a6→6fc2108→e9d534d`+offload Windows 修复）+ venv `G:\qwen3.8model\vllm-win029` |
| 配置 | GSQ int3 + DFlash2 gptq3c N=2 + nvfp4 KV + PIECEWISE + capture-sizes 3 + prefix-cache + **手动池 3,400,000,000**（121,058 tokens）+ max-model-len 110,000 + seqs 1 |
| 端口/名 | 127.0.0.1:8080 / `qwen3.8-27b-gsq`（与旧服务一致，客户端零改动） |

与旧配方（`run_dflash2_n2.cmd`，0.27.1）的差异（全部有意）：
1. venv 换 `vllm-win029`，PYTHONPATH overlay 移除（自研已全部迁入底座仓）。
2. spec 参数换 0.29 原生 `--speculative-config.method/model/num_speculative_tokens`（argv 探针实证）。
3. flashinfer 0.6.18 runner 契约：`FLASHINFER_WORKSPACE_BASE=C:/fw`（**正斜杠**）+ tvm_ffi LDFLAGS 指向 vllm-win029；`VLLM_HAS_FLASHINFER_CUBIN` 移除（0.27 时代旗标）。
4. **`--kv-offloading-*` 保留（9-26 晚已修复回填）**：offload×Windows 崩溃根因=cuMemcpyBatchAsync 驱动缺陷（非默认流即毒化），底座 `d7cdb91` 以逐条 `cuMemcpyAsync` 绕开（步骤 021）；多轮前缀场景实证（32k 二轮 TTFT 21.0→3.1s）。若未来移除该修复，offload 必须同步关掉（步骤 019 三探针证据）。
5. TMP/TEMP 重定向 `G:\qwen3.8model\_tmp_prod029`；启动期清扫 `vllm_offload_*.mmap` 残留（防崩溃残留占盘）。

## 2. 环境契约（重装环境必重放）

1. VS：`call "C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools\VC\Auxiliary\Build\vcvars64.bat"`（humming/triton JIT 要 cl+ninja）
2. `HOME=USERPROFILE=C:\fi`（humming 缓存键的一部分）；`CUDA_HOME=CUDA_PATH=C:\PROGRA~1\NVIDIA~2\CUDA\v13.3`（8.3 短路径）
3. `VLLM_KV_CACHE_LAYOUT=HND`；`FLASHINFER_WORKSPACE_BASE=C:/fw`（正斜杠）；`FLASHINFER_EXTRA_LDFLAGS=-LG:/qwen3.8model/vllm-win029/Lib/site-packages/tvm_ffi/lib -ltvm_ffi`
4. **cwd 铁律**：绝不在 `vllm-030win-git` 记录仓目录下起 python（cwd 会捕获仓内 0.27.1 源码树炸 `vllm._C_stable_libtorch`）；一律 cd 到 `G:\qwen3.8model` 或模型目录再起。
5. flashinfer 0.6.18.post1（纯 py JIT 包）已装 venv；humming 八件套垫片见 `tools/shims/SHIMS.md`。

## 3. 切换步骤

1. 停旧服务：按命令行杀净（`tools/kill_vllm_orphans.ps1`——EngineCore spawn 孤儿用系统 Python 路径，必须按 CommandLine 匹配 `cli.main serve|multiprocessing.spawn|spawn_main` 全杀，Path 过滤必漏）；确认 8080 端口释放。
2. 起新服务：`cd G:\qwen3.8model` 后执行 `run_dflash2_n2_base029.cmd`（或直接 call 真源）。启动约 3-4 分钟（AOT 缓存热时约 3 分钟）。
3. 验证四件：①`GET /v1/models` 返回 `qwen3.8-27b-gsq`/max_model_len 110000 ②chat 请求带 `message.reasoning` 且 finish=stop ③tool 请求 finish=tool_calls 带结构化 arguments ④任一 ≥8k 请求 needle 命中（77349）。
4. 日志核对三行：`GPU KV cache size: 114,974 tokens`（生产口径=mamba align 生效态；测量口径 121,058 是 mamba none 记账，两者都确定性复现）、`decode_backend=xqa, kv_cache_dtype=torch.uint8`、`Capturing CUDA graphs (PIECEWISE)`（含 `Capturing model for DFlash2 speculator`）。

> **执行记录**：2026-09-26 15:44 已切换（步骤 020）——服务分离运行于 8080，四件验证+三行核对全过；日志 `G:\qwen3.8model\prod029_logs\serve_prod029.log`。

## 4. 回退步骤

1. 杀净 0.29 服务（同上按命令行杀）。
2. 直接起旧配方 `G:\qwen3.8model\Qwen3.8-27B-3Bit-GSQ\run_dflash2_n2.cmd`（0.27.1 venv `vllm-win` 冻结只读 + overlay tag `freeze-migration-20260925`，未动分毫）。
3. 回退锚数据不变：A5b/A5/A2/A4 全部是旧栈采的（`锚点数据包/`），复现即回退成功。

## 5. 演练记录（2026-09-26，步骤 019）

**判定树（三探针二分）**：
| 探针 | 配置 | 8k 请求结果 |
|---|---|---|
| A | 生产全量（prefix+offload+spec） | ❌ 首个长请求空返，引擎崩（`illegal memory access`） |
| B | 生产去 spec（prefix+offload） | ❌ 同崩 ⇒ 与 spec 无关 |
| C | 生产去 offload（prefix+spec） | ✅ 全绿（8k×2 needle 中，二发 TTFT 5.8→3.1s=prefix 命中） |

**定罪**：`--kv-offloading-*` 的 KV 转存路径。崩溃暴露点=编译后 backbone 的 triton 内核（inductor `static_triton_launcher.C_impl._launch_kernel`，异步报错模式，offload transfer 的事件同步是首个发现点而非真凶帧）。已排除：钉页（独立探针 `cudaHostRegister` 返回 success 且 GPU→host 拷贝正常）、spec、prefix-cache。Windows 移植层两坑已修（base 仓 `b315e77`+后续：`/dev/shm`→tempfile 路径、Windows 禁删已映射文件⇒unlink 推迟到 cleanup、madvise 缺失⇒numpy 回退）；残余缺陷在 0.29 offload 子系统更深面（转存几何/内核指针），挂账待查。

**最终配置冒烟（=生产配方）**：health ✅；chat+reasoning ✅（reasoning 242 字符、finish=stop）；tool-call ✅（`{"city":"上海"}`）；8k×2 needle ✅ + prefix 命中 ✅；32k needle ✅（TTFT 21.8s、稳态 63.4）；spec 活性 ✅（/metrics 接受率短请求 0.56、haystack 32k 0.52——真实请求 A4 带 0.68-0.80 已在步骤 018 同引擎配置验收）；**共享显存 per-process 0 MB 无溢出**；池账 121,058 tokens。

## 6. 未切换面（挂账）

1. **vision 部署**（`run_qwen_vision_offload_157696_best.cmd` 仍 0.27 栈）：A7 vision 131k/155k 锚未采，无验收基线不切换。
2. ~~**⭐【高优先待优化】KV offload 缺陷**~~ **✅ 已修复回填（步骤 021，底座 `d7cdb91`）**：根因=cuMemcpyBatchAsync 驱动级缺陷（非默认流使用即毒化上下文）；修复=`_custom_ops.swap_blocks_batch` Windows 分支 ctypes 逐条 `cuMemcpyAsync`（单点收口、保留流序与转存流水）。生产已带 offload 运行，多轮前缀实证 32k 二轮 TTFT 21.0→3.1s。
3. MiniCPM5-2B+DSpark+BGE-M3 共存服务独立维护（0.29 侧 `serve_minicpm5_2b_dspark_base029.cmd` 已冒烟通过，S3a）。
